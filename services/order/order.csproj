<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Chốt tên assembly để output publish (order.dll) khớp ENTRYPOINT của Dockerfile,
         không phụ thuộc casing thư mục (NTFS case-insensitive). -->
    <AssemblyName>order</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- ADO thuần, không EF Core: order chỉ cần ping DB (`SELECT 1`) cho readiness probe. -->
    <PackageReference Include="Npgsql" Version="10.0.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\packages\Shop.Contracts\Shop.Contracts.csproj" />
  </ItemGroup>

</Project>
