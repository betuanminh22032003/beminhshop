# Mẫu cấu hình cho docker-compose.yaml — COPY thành .env rồi đổi giá trị thật:
#   cp .env.example .env
# .env bị gitignore (secret không bao giờ commit); file này chỉ phơi HỢP ĐỒNG cấu hình.
POSTGRES_USER=shop
POSTGRES_PASSWORD=replace_me
POSTGRES_DB=shop
POSTGRES_PORT=5432
